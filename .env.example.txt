# Get these from https://dashboard.stripe.com/apikeys (use the TEST keys while developing)
STRIPE_SECRET_KEY=sk_test_51xxxxxxxxxxxxxxxxxxxxxxxx

# Get this after creating a webhook endpoint at https://dashboard.stripe.com/webhooks
# Point it at:  https://your-domain.com/webhook   (event: checkout.session.completed)
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxx

# The app (frontend + API) runs as one server on this port
PORT=3000

# Only needed if you split the frontend onto a different host/port later.
# Left blank, it defaults to http://localhost:$PORT.
FRONTEND_URL=

# Local: postgres://youruser:yourpassword@localhost:5432/kilnthread
# Hosted (Render/Railway/Supabase all give you this string after creating a Postgres instance):
DATABASE_URL=postgres://user:password@host:5432/kilnthread

# Long random string — used to sign customer/admin login tokens.
# Generate one with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

# SMTP — any provider works (SendGrid, Postmark, Resend, Mailgun, AWS SES,
# or Gmail with an app password for testing). These example values are for
# Gmail SMTP; swap for your provider's host/port.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-address@gmail.com
SMTP_PASS=your-16-character-app-password
MAIL_FROM="Kiln & Thread" <orders@kilnandthread.com>
